import { Login } from "./Login"

export default function Header() {
    // Here we can construct the entire header. Add menu, icons, some title perhaps. Whatever you like.
    return (
        <div className="header">
            <Login />
        </div>
    )
}
